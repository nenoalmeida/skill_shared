<h1>skillshared</h1>

<p>The better place to learn something </p>
<p>
  Hello <%= current_user.email %>
  <% if current_user.image.present? %>
    <%= image_tag(current_user.image, size: "40") %>
  <% end %>
  </p>
</br>
<%= link_to "Sair", destroy_user_session_path, method: :delete %>